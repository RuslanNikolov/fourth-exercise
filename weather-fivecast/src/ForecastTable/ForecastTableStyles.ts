// in some instances (not on this project) its better to use a CSS-in-JS method,
// it gives you some flexibility like passing props to the style, etc
export const tableHeadRow = {
    fontWeight: 'bold'
}